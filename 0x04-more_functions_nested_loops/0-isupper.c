#include "main.h"
#include <stdio>
/**
 * _isupper - check if a letter is uppercase
 * 
 * Return: 1 for upper letter or 0 for any else
 */

int _isupper(int a)
{ 

	if (x = 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}	
