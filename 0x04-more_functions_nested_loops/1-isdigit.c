#include "main.h"


/**
 * main - write to check for a digit
 *
 * Return: Always 0

 int _isdit(int c))

{

   char c  = '6';
   if (isdigit (c))
   {
   printf("%c is a digit\n", c);
   }
   else
   {
   printf("%c is not a digit\n", c);
   }
   return 0; 
}
